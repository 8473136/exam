<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.guozhi.mapper.QuestionMapper">
    <select id="getQeustions" resultType="com.guozhi.rvo.QuestionRVO">
        SELECT
            id,
            question_name,
            question_source,
            create_time,
            (select dict_name from tab_sys_dict where id = q.question_type) questionTypeName,
            (select dict_name from tab_sys_dict where id = q.question_depot) questionDepotName,
            (select dict_name from tab_sys_dict where id = q.special_id) specialIdName
        FROM
            `tab_exam_questions` q
        WHERE q.is_deleted = 0
    </select>
</mapper>